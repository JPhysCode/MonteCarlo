#ifndef CAPTURE_H
#define CAPTURE_H

#include "physics.h"

// Neutron capture cross-sections and calculations

// Mark a neutron as captured
void neutronCapture(Neutron& neutron);

#endif // CAPTURE_H
